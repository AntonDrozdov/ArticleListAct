@{
    int anum = 0;
    int acount = ViewBag.ArticlesVM.Count;
    double tmp = acount / 3;
    int tmp2 = (int)Math.Floor(tmp);
    int cols = (acount % 3 == 0) ? (acount / 3) : (tmp2 + 1);//по три картинки в ряд
    cols = (cols > 3) ? 3 : cols;
}

<div class="col-xs-12 col-sm-9">
    @for (int j = 0; j < acount; j++)
    {
        @await Component.InvokeAsync("ArticleTile", new { avm = ViewBag.ArticlesVM[j] })
    }
</div>

<div class="col-xs-12 col-sm-3">

</div>



<div class="col-xs-12 ">
    @await Component.InvokeAsync("SearchHashTagPagination", new { pi = ViewBag.PagingInfo })
</div> 